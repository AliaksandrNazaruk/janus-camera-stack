# janus_camera_page — Python dependencies
#
# This file provides version-range constraints for reproducible installs.
# For locked deployments, run:
#   pip install -r requirements.txt
#   pip freeze > requirements.lock
# and deploy requirements.lock to production.
#
# Python: matches top-level pyproject.toml >=3.12 (mono-repo runtime floor).
# Standalone L4 may run on >=3.10 (fastapi 0.104+ supports it), but mono-repo
# CI / Ansible deployment assumes 3.12+.

# ── Web framework ─────────────────────────────────────────────────────
fastapi>=0.138.1,<0.139.0
uvicorn[standard]>=0.49.0,<0.50.0
starlette>=1.3.1,<1.4.0

# ── HTTP client ───────────────────────────────────────────────────────
httpx>=0.25.0,<0.29.0         # async HTTP — all Janus, proxy, and cross-node communication

# ── Data validation ───────────────────────────────────────────────────
pydantic>=2.13.4,<3.0

# ── Observability ─────────────────────────────────────────────────────
prometheus-client>=0.25.0,<1.0

# ── WebSocket protocol ────────────────────────────────────────────────
websockets>=16.0,<17.0        # Janus WS proxy (routes/janus.py)

# ── Templates ─────────────────────────────────────────────────────────
jinja2>=3.1.0,<4.0            # used by routes/templates.py (player HTML rendering)

# ── Optional: depth_camera node only ─────────────────────────────────
# Uncomment on the depth camera Raspberry Pi node:
# pyrealsense2>=2.54.0        # Intel RealSense D435/D435i SDK
# numpy>=1.24.0,<3.0          # frame buffer operations in realsense_mux.py
