# Example contract.env для generic V4L2 adapter.
# Ansible-managed — PORT must match Janus mountpoint config.

# RTP destination port на Janus (must match Janus mountpoint config).
# Allocate uniquely per camera (don't collide с rtp-rgb:5004, rs-stream depth:5006, etc.).
PORT="5020"
